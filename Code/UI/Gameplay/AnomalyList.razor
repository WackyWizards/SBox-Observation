@using Sandbox.UI;

@namespace Observation.UI
@attribute [Hud( true )]
@inherits Panel

<root>
	@foreach ( var anomalyType in AvailableTypes )
	{
		<button onclick="@( () => Report( anomalyType ) )">@anomalyType.GetLocalizedName()</button>
	}
</root>
