@using Sandbox.UI

@namespace Observation.UI
@attribute [Hud( true )]
@inherits Panel

<root>
	<label @ref="Report">Verifying Report...</label>
</root>

@code {
	#nullable enable
    
	public Label? Report;

	public void SetReport( bool validReport )
	{
		if ( Report.IsValid() )
			Report.Text = validReport ? "Anomaly Cleared!" : "No Anomalies Found!";
	}
}
